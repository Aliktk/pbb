# Track T0 — Foundation — iteration 1

CHANGED
- Monorepo scaffold: `package.json` (pnpm workspaces), `pnpm-workspace.yaml`,
  `tsconfig.base.json`, `.gitignore`, `.npmrc`, `docker-compose.yml`.
- `packages/types` — shared Zod DTOs + the ONE eligibility-state list (INV-5 source).
- `apps/api` — NestJS scaffold: `main.ts` (helmet, /api/v1 prefix, 422 validation),
  `app.module.ts` (config, throttler, prisma), Prisma module/service, `/health`,
  `NotificationPort` + console driver, `StoragePort` interface.
- `apps/api/prisma/schema.prisma` — EVERY §3 entity + all tables any later track needs
  (auth tokens, invitations, OTP, content versions, call tracking, stock, notifications).
  Schema is now FROZEN (post-freeze changes are a stop-condition).
- Migrations: `0001_init` (generated from schema, 25 tables/enums) and
  `002_eligibility_view_and_audit_guard` (hand-authored: the `donor_eligibility` view +
  the append-only `audit_log` triggers).
- `apps/api/prisma/seed.ts` — 14 towns, 6 branch offices, 8 roles, 200 donors spread
  across all seven eligibility buckets; deterministic IDs.
- `docs/` — Harness prompt, Build plan, Route inventory (59 screens), Deployment
  (Supabase + Vercel), this handoff.

ADDED (gaps filled under standing authority — see ROUTE-INVENTORY.md §gaps)
- `Invitation`, `PasswordResetToken`, `RefreshToken`, `DonorOtp` tables (auth flows the
  design implied but did not model).
- `PageVersion`, `RequestCall`, `StockLevel`, `NotificationLog`, `Message` tables.
- `directUrl` split on the datasource (required for Prisma ↔ Supabase pooler).
- Docker Postgres republished on **5433** (native Postgres occupies 5432 on this machine).

PASSING (verified against real Postgres 16 in Docker this session)
- `prisma validate` → schema is valid.
- `prisma migrate diff` generated `0001_init`; both migrations apply cleanly (25 tables).
- **INV-5 proven:** a 7-case fixture returns exactly `REMOVED, DEFERRED, NEVER_SCREENED,
  REACTIVE, SCREENING_STALE, COOLDOWN, ELIGIBLE` — 7/7 distinct states from the view.
- **INV-12 proven:** `INSERT` into `audit_log` succeeds; `UPDATE` and `DELETE` are both
  refused at the database level ("audit_log is append-only").
- Password auth to the container over TCP confirmed working.

FAILING
- None in what was built. See BLOCKED for what could not be run here.

ASSUMED
- API host = an always-on Node service (Railway/Render/Fly), not Vercel serverless, so
  BullMQ workers run. Web = Vercel. DB = Supabase. (docs/DEPLOYMENT.md — revisit with client.)
- `Donor.branchId` is required and `@@unique([branchId, mrNo])` realises "MR unique per
  branch" (§3 listed only townId; a branch scope was needed for MR numbering).
- Six offices = Quetta (head), Pishin, Zhob, Loralai, Chaman, Sibi; other 8 towns served
  from these. (§3 says "six branch offices" without naming them.)
- Eight roles + their permission matrix are a first cut (T1 owns the authoritative matrix).

BLOCKED (need the client / their machine — never silently skipped)
- Full `pnpm install` + `prisma generate` + `nest build` + `next build` + `seed.ts` run
  were NOT executed here (native module builds e.g. argon2, and OneDrive I/O, make this a
  your-machine step). Run: `node scripts/setup-local-env.mjs && pnpm install &&
  docker compose up -d db && pnpm db:reset` to reproduce the full gate locally.
- API-hosting decision (Vercel serverless vs always-on Node) — docs/DEPLOYMENT.md.
- The §10 client decisions (founder line, 2013–2026 figures, photos, WhatsApp number,
  public self-registration, data-retention window, privacy-undertaking signer).

NEXT (smallest next slice)
- Run the full local gate once (`pnpm install` → `pnpm db:reset` → `pnpm build`) to
  confirm client generation + seed on the real machine, then start Wave 1: T1 (Auth/RBAC)
  and T2 (Donor domain) in parallel — they own disjoint files (see BUILD-PLAN.md).
